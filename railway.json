{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && npm install && npx prisma generate"
  },
  "deploy": {
    "startCommand": "cd backend && npx prisma db push && node src/server.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
